import { CodeBlock } from "@/components/CodeBlock";
import { ComponentPreview } from "@/components/ComponentPreview";
import { PropsTable } from "@/components/PropsTable";
import {
  DocumentDetailToolButton,
  DocumentDetailTooltip,
  IconCopy,
  tooltipCss,
} from "@globalise/design-system";

# Tooltip

Small React Aria tooltip wrapper for dense document-viewer controls.

## Preview

<ComponentPreview className="bg-neutral-900">
  <DocumentDetailTooltip label="Copy scan URI">
    <DocumentDetailToolButton
      aria-label="Copy scan URI"
      icon={<IconCopy className="h-s16 w-s16" />}
    />
  </DocumentDetailTooltip>
</ComponentPreview>

## Usage

<CodeBlock
  code={`<DocumentDetailTooltip label="Copy scan URI">
  <DocumentDetailToolButton aria-label="Copy scan URI" icon={<IconCopy />} />
</DocumentDetailTooltip>`}
/>

## CSS Recipe

<CodeBlock code={tooltipCss} language="css" />

## Props

<PropsTable
  props={[
    {
      name: "label",
      type: "ReactNode",
      description: "Tooltip content.",
    },
    {
      name: "placement",
      type: "TooltipProps['placement']",
      default: '"bottom"',
      description: "React Aria tooltip placement.",
    },
    {
      name: "delay",
      type: "number",
      default: "350",
      description: "Open delay in milliseconds.",
    },
  ]}
/>
