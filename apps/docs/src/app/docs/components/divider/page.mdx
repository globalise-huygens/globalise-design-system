import { ComponentPreview } from "@/components/ComponentPreview";
import { PropsTable } from "@/components/PropsTable";
import { CodeBlock } from "@/components/CodeBlock";
import {
  Divider,
  SectionDivider,
  Section,
  Container,
} from "@globalise/design-system";

# Divider

A horizontal rule component with the brand's semi-transparent white styling. `SectionDivider` wraps `Divider` inside a `Container` with vertical spacing.

## Divider Preview

<ComponentPreview>
  <div className="w-full grid gap-4">
    <Section>
      <Container>
        <Divider color="light" />
      </Container>
    </Section>
    <Section background="light" spacing="default">
      <Container>
        <Divider color="dark" />
      </Container>
    </Section>
  </div>
</ComponentPreview>

## Usage

<CodeBlock code={`import { Divider, SectionDivider } from "@globalise/design-system";

{/* Simple divider */}

<Divider />

{/* Full-width section divider with container + spacing */}

<SectionDivider />`} />

## SectionDivider

`SectionDivider` combines a `Container` with a `Divider` and adds `my-[100px]` vertical margin. Use it between major page sections. This is probably the most common way to use dividers in the design system.

## Props

### Divider

<PropsTable
  props={[
    {
      name: "className",
      type: "string",
      description: "Additional CSS classes",
    },
  ]}
/>

### SectionDivider

<PropsTable
  props={[
    {
      name: "className",
      type: "string",
      description: "Additional CSS classes (applied to Container wrapper)",
    },
  ]}
/>
