import { ComponentPreview } from "@/components/ComponentPreview";
import { PropsTable } from "@/components/PropsTable";
import { CodeBlock } from "@/components/CodeBlock";
import {
  EntityBadge,
  IconEntityCommodity,
  IconEntityDate,
  IconEntityDimensions,
  IconEntityDocument,
  IconEntityOrganisation,
  IconEntityPerson,
  IconEntityPlace,
  IconEntityShip,
} from "@globalise/design-system";

# Entity Badge

A reusable badge for marking entity types in transcription text, headers, and entity summaries. Use EntityTag when the text itself is a linked entity value.

## Preview

<ComponentPreview>
  <div className="flex flex-wrap gap-3">
    <EntityBadge type="ship" icon={<IconEntityShip className="h-s16 w-s16" />}>
      Ship
    </EntityBadge>
    <EntityBadge
      type="person"
      icon={<IconEntityPerson className="h-s16 w-s16" />}
    >
      Person
    </EntityBadge>
    <EntityBadge
      type="place"
      icon={<IconEntityPlace className="h-s16 w-s16" />}
    >
      Place
    </EntityBadge>
    <EntityBadge
      type="commodity"
      icon={<IconEntityCommodity className="h-s16 w-s16" />}
    >
      Commodity
    </EntityBadge>
    <EntityBadge
      type="dimensions"
      icon={<IconEntityDimensions className="h-s16 w-s16" />}
    >
      Dimensions
    </EntityBadge>
    <EntityBadge
      type="organisation"
      icon={<IconEntityOrganisation className="h-s16 w-s16" />}
    >
      Organisation
    </EntityBadge>
    <EntityBadge type="date" icon={<IconEntityDate className="h-s16 w-s16" />}>
      Date
    </EntityBadge>
    <EntityBadge
      type="document"
      icon={<IconEntityDocument className="h-s16 w-s16" />}
    >
      Document
    </EntityBadge>
  </div>
</ComponentPreview>

## Usage

<CodeBlock code={`import { EntityBadge, IconEntityShip } from "@globalise/design-system";

<EntityBadge type="ship" icon={<IconEntityShip className="h-s16 w-s16" />}>
  Ship
</EntityBadge>`} />

## Props

<PropsTable
  props={[
    {
      name: "type",
      type: '"ship" | "person" | "place" | "commodity" | "dimensions" | "organisation" | "date" | "document"',
      default: '"document"',
      description: "Entity type, determines badge color and outline style.",
    },
    {
      name: "icon",
      type: "ReactNode",
      description:
        "Optional icon rendered before the label. Typically `h-s16 w-s16`.",
    },
  ]}
/>
